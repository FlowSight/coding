1. give following prompt to AI before coding(at start):
   1. for each class : can you add comment describing the purpose, lifecycle, connectivity with other classes
      1. for each method in the class, describe the i/o, logic,
   2. Do the above only if no comment or a comment < 1sent. If >1 line comment exist, dont create comment.
2. After AI generates comment, still verify the comment and logic for any class/method u eyeball
3. If any code pattern (like decorator) seems unfamiliar, ask AI to explain, communicate this to interviewer

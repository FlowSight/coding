Debug: Mako Template Engine
Challenge Description
In this test, you need to find and fix errors in a Python template library. You will get a link to a GitHub folder. This folder has a version of the Mako template engine that contains hidden bugs. Your job is to find these bugs using the test suite and fix them.

This test checks if you can:

Work with new code you haven't seen before.
Understand complex logic quickly.
Use debugging tools well.
Explain your thoughts clearly while you work.
Repository: Google Drive

Interview Details
Time: About 45-60 minutes
Setup: Download the code, work on your own computer, and share your screen
Language: Python
Action: The interviewer will watch you debug. You must talk out loud as you solve the problem.
About Mako
Mako is a popular tool for Python. It turns templates into Python code so they run very fast. Here are its main features:

Embedded Python: You can put Python code inside templates using <% ... %>.
Variable Interpolation: You can use ${variable} to put values into templates.
Template Inheritance: Templates can share a layout using <%inherit>.
Reusable Components: You can make functions inside templates using <%def>.
Control Flow: Standard Python loops and if statements work normally.
Example Template:


<%inherit file="base.html"/>

<%def name="greeting(name)">
    Hello, ${name}!
</%def>

% for item in items:
    <li>${item}</li>
% endfor
You do not need to know Mako before taking this test. The code comes with many tests that will show you what is broken.

How to Begin
Installation Steps
Download the code and go to the debug-mako folder.
Install the needed libraries: pip install -r bugsquash-requirements.txt
Run the tests: python -m pytest test/
Look at which tests fail and start checking the code.
Folder Layout
The folder contains all the code for the library:


mako/
├── template.py      # Main class for rendering templates
├── lexer.py         # Breaks template code into tokens
├── codegen.py       # Turns parsed templates into Python code
├── ast.py           # AST node definitions
├── _ast_util.py     # Helpers for changing ASTs
├── lookup.py        # Finds and loads template files
├── runtime.py       # Tools used while the template runs
├── parsetree.py     # Definitions for the parse tree
├── pygen.py         # Generates Python source code
├── pyparser.py      # Parses Python expressions
├── filters.py       # Functions to filter output
├── cache.py         # System for caching templates
├── exceptions.py    # Custom error classes
├── cmd.py           # Command-line tools
├── compat.py        # Python version compatibility
└── util.py          # General helper tools

test/
├── test_template.py # Tests for rendering templates
├── test_lexer.py    # Tests for the Lexer
├── test_lookup.py   # Tests for finding files
├── test_ast.py      # Tests for AST handling
└── ...              # Other test files
How to Find Bugs
Steps to Follow
Run All Tests: See the big picture of what is failing.

python -m pytest test/ -v
Focus on One Failure: Run just the broken test to get clearer output.

python -m pytest test/test_lookup.py::TestClass::test_name -v
Read the Test: Understand what result the test expects.

Trace the Code: Follow the logic from the start to where it crashes.

Use Print or pdb: Add breakpoints or print statements to check values.

import pdb; pdb.set_trace()
Check Changes: Look for code that looks strange or inconsistent.

Common Error Types
Most bugs in this test fit into these groups:

Wrong Conditions: Logic checks (like if statements) that do the opposite of what they should do.
Missing Edge Cases: Code that fails when it gets unexpected input.
AST/Parse Tree Issues: Errors in how the code creates nodes or changes data structures.
String Errors: Mistakes in how the code handles text or template syntax.
💡 Deep dive into System Design concepts with clear explanations and visual diagrams. Check out ByteByteGo's courses on System Design Interview.Use promo code 1p3acre to get 10% exclusive discount！


https://drive.google.com/file/d/1xBoI7fMZx5M-4t4R2ITPDc1iI_eTh7Py/view
Given an integer s, find the number of all four-digit numbers whose digit sum equals s. Implement a function countFourDigitCodesWithSum that takes an integer s (0 ≤ s ≤ 36) as input and returns the count of such four-digit numbers.

Example
Input
10
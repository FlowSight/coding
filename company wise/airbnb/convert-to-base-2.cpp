// LC : 1017
// 17apr2026
//sev0 : didnt get at first


// for a base 2 no, if u convert it to base -2 then :  for each odd pos i.e. 1,3, you its decreasing
// by (1<<pos) so diff = 2*(1<<pos). so you add that diff to orig no until no diff.
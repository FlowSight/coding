Design a group chat system that supports sending messages to all group members, receiving messages, and viewing chat history.


Design a job scheduler system that supports adding, deleting, querying, and priority scheduling of jobs. The system should handle concurrent and scheduled tasks.


Write a function that takes a function func and a maximum retry count max_retries, and attempts to execute the function. If execution fails, retry until it succeeds or the maximum number of retries is reached. Implement the interface: retry(func: Callable[[], Any], max_retries: int) -> Any.


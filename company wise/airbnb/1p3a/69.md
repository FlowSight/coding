Storefront: Connect 4
Many posts have already discussed this. Note that you need to print the grid. There are about 10 test cases. If you've solved this problem before, it shouldn't be a problem. In the follow-up, they asked if it could be optimized. I suggested using an extra array to record how many disks are in each column, so we don't have to consider the effect of gravity every time. The developer thought it was a good idea. A week later, I was notified that I passed and we scheduled a VO (Voice over Entrance

). Coding: Implement a retryer that returns the result if successful; if it fails, retry a maximum of 5 times, with a 5ms interval between each retry;
if it still fails after 5 retries, throw the last error; another requirement is that the retry interval increases progressively: 5ms, 10ms, 15ms, ..., 25ms. In Java, it would look something like this:
`public static <T> T withRetry(Supplier<T> func) { }`
The input consists of two lambda expressions: `
String res1 = withRetry(() -> "Success");
String res2 = withRetry(() -> {
throw new RuntimeException("Always fail");
});

` Code Review: The interviewer briefly explained the requirements and then gave me three pull requests (PRs). There was basically no interaction. The first two were relatively short, but the third was extremely long, so be careful with your time allocation. Although the interviewer said it wasn't necessary to read them all, and that finding the most issues was more important, I felt the third PR had significantly more points. Anyway, there was no feedback, so I don't know how this round fared. I remember seeing materials from HR, including a Git repository that discussed several APIs for Atlantis. The PRs that needed review were basically about calling those APIs. The first PR seemed to be fixing a consistency level parameter, the second adding pagination, and the third mentioning Atlantis complaining about our frequent API calls and suggesting adding a batch job.

Technical Experience Interview: This round felt like a deep dive into my past projects, asking many detailed questions until they heard what they wanted to hear. For example, what's your proudest project? What was the most challenging part? How did you work with your teammates? How did you resolve conflicts? What have you learned from it?

System Design: Design a listing booking system where users can book available listings; if a listing is booked, the user can join the waitlist; and when a listing becomes available, the first 20 users need to be notified.

The interviewer, a guy from Eastern Europe, said that services for property pricing, posting, searching, and notifications are readily available. He was more interested in the data schema and how the two main functionalities would be implemented.

I received a rejection letter a week later, with no feedback. Seeking advice from those who have interviewed me!


Write a program to print the current grid of a Connect 4 game. Provide 10 test cases that reflect various states of the grid changes. The grid has 6 rows and 7 columns, using '.' to represent empty slots, and 'X' and 'O' to represent the players' discs. Ensure the program can correctly display the grid's state.

Implement a retryer function; if successful, return the result. If it fails, retry up to 5 times with increasing intervals: starting at 5ms, then 10ms, 15ms, ..., until 25ms. If after 5 attempts it still fails, throw the last error. The function input is two lambda expressions, returning success or throwing an exception.


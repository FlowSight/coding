The first part was a project deep dive,

and the second part was coding.
Given a list of tasks (task ID, task deadline as an integer, task reward), each task requires one unit of time to complete. The task is to return the order in which tasks are completed to maximize the reward.
For example, input [("a", 2, 8), ("b", 1, 3), ("c", 2, 5), ("d", 3, 3)],
output is "c", "a", "d" or "a", "c", "d".
Once you know the method, writing it isn't difficult, but I struggled a lot with the optimal runtime algorithm because the methods used weren't very common.
A greedy approach wouldn't work because some tasks, even with early deadlines, have low rewards and should be abandoned.
Backtracking and brute-force search have exponential runtime, which the interviewer said wasn't fast enough.
The solution is to sort by deadline, then scan through the selections, adding a task to the task selections each time the deadline increases, otherwise replacing the task with the lowest reward in the current selection. This can achieve an O(n^2) time complexity. Follow-up: How to improve to O(n log n)? Using a heap, you still need to complete the code; just completing n^2 shouldn't be enough to pass.

The third round was system design: design a group chat system. The focus was on database schema design to implement the most basic chat app functionality, such as returning the top 20 group chats with the most recent messages. Then, the discussion focused on how to send notifications to online and offline users each time a new message is sent, using client-side long polling or web sockets + asynchronous message queues. The fourth round, the so-called Airbnb core value, was essentially the most common behavioral questions: why you're interested in Airbnb , what drives you in your career , example of balancing interests among multiple stakeholders (basically asking how you resolved conflicts or disagreements, one of the most common behavioral questions), proudest achievements, biggest difficulties faced , feedback received and how you handled them, biggest failures, experience mentoring others ... In the second round, coding, I was sure I'd fail, but the interviewer was a kind Chinese guy who practically gave me the answers. It took me 20 minutes to come up with the optimal solution, but I finished it in 20 minutes, so I passed. Unfortunately, I didn't match the team I wanted, so I'll probably take other offers ... (Added 2025-02-06 00:51 +08:00): I'd forgotten, there was also a code review interview at the beginning. They gave you a few code PRs and asked you to add comments. I think the more points you answered correctly, the better. There wasn't much interaction; it was just reading code, finding problems, and writing comments.



Given a list of tasks, where each task is defined by a task ID, a task deadline (as an integer), and a task reward. Each task requires one unit of time to complete. Write an algorithm to maximize the task rewards and return the optimal order of task completion.

For example, if the input is [('a', 2, 8), ('b', 1, 3), ('c', 2, 5), ('d', 3, 3)], the possible outputs are [c, a, d] or [a, c, d].

The requirement is to write an optimized algorithm to solve the problem. Avoid using a greedy algorithm. Improve the solution using specific data structures and implement.

Input:
A list of tasks, where each task is a tuple of (task_id, task_deadline, task_reward).
Output:
A list of task IDs indicating the optimal task completion order to maximize the reward.
Example:
Input: [('a', 2, 8), ('b', 1, 3), ('c', 2, 5), ('d', 3, 3)]
Output: [c, a, d] or [a, c, d]
Constraints:
Up to 1000 tasks.
Constraints:
Each task can only be completed before its deadline.
Hint:
Optimize the algorithm by using sorting and data structure improvements to reduce complexity.

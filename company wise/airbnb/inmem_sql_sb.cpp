// Design an in-memory SQL database.

// Requirements:

// The database must support basic CRUD operations.
// The database design should be scalable to support multiple data types.
// Provide sample interface designs for inserting (insert), deleting (delete), querying (select), and updating (update) data.
// Please design the data structure and describe the core implementation logic.
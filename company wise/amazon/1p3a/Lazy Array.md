mplement a Lazy Array, which computes elements only when they are accessed, thus reducing unnecessary computations. Ensure that only necessary and uncomputed elements are calculated during access. Provide a method to access elements and a preprocessing method.

Example
Input
2